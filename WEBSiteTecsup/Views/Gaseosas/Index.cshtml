@model IEnumerable<WEBSiteTecsup.Models.Gaseosas>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.GaseosaNombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GaseosaMarca)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.GaseosaNombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GaseosaMarca)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GaseosaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GaseosaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GaseosaID })
        </td>
    </tr>
}

</table>
